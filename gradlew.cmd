@echo off
REM No-op Gradle wrapper for Expo-managed app.
REM This repository does not require local Gradle builds.
echo gradlew.cmd: no-op shim for Expo-managed app (no native Gradle build).
exit /B 0
