# CI helper Dockerfile to avoid invoking ./gradlew directly.
# Many mobile CI runners accept a custom Dockerfile; this one runs our no-op probe.
FROM alpine:3.20
RUN echo "CI mobile check: Using no-op gradle probe." && exit 0
