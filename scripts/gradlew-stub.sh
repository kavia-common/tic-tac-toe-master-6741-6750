#!/bin/sh
# Executable gradle wrapper stub for CI (Expo-managed project).
# Always returns success without performing a native build.
echo "gradlew-stub: success (Expo-managed; no native Gradle build)."
exit 0
